use psd;
CREATE TABLE psd(
    id INT(11) NOT NULL AUTO_INCREMENT,
    name VARCHAR(45),
    l VARCHAR(45),
    t VARCHAR(45),
    w VARCHAR(45),
    h VARCHAR(45),
    sid VARCHAR(45),
    PRIMARY KEY(id)
) DEFAULT CHARSET=utf8;
