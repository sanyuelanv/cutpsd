<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
    <link rel='stylesheet' href='/css/style.css' />
    <style>
        .show{width: 360px;height: 630px;margin: 30px auto;overflow-x:hidden;position: relative;}
        .page-header{margin-top: 70px;}
        .main{width: 720px;height: 1280px;transform: scale(0.5); left: 0px;-webkit-transform-origin: 0 0;transform-origin: 0 0;}
        .show *{display: block;position: absolute;-webkit-transform-origin: 0 0;transform-origin: 0 0;}
    </style>
</head>
<body>
<div class='top'><a href="/">返回首页</a></div>
<div class="page-header">
    <h1>效果图：<small><%= name %></small></h1>
</div>
<div class="show">
    <div class="main">
        <% if (data.length) { %>
            <% data.forEach(function(item){%>
                <div style="left:<%= item.l%>px;top:<%= item.t%>px;width:<%=item.w%>px;height:<%= item.h%>px;background-image:url(/out/<%= item.name%>.png)"></div>
            <% }) %>
        <% } %>
    </div>
</div>

</body>
</html>
